Express the following known digital algorithm as Boolean expression trees. Use your existing algorithm knowledge. Return only one JSON object; no prose, reasoning, Markdown, executable code or truth table.

Add the three unsigned one-bit inputs a, b and carry_in. Return the two-bit exact sum, least-significant bit first.
Input wire indices: 0=a, 1=b, 2=carry_in.
Output list order: 0=sum, 1=carry_out.
Each output is one nested JSON array. The complete grammar is: ["input", integer_wire_index], ["constant", 0_or_1], ["not", expression], ["and", expression, expression], ["or", expression, expression], ["xor", expression, expression]. All and/or/xor nodes have exactly two operands. Use literal integer 0 or 1 for constants, not JSON true/false. No named references, local definitions or other operators are supported. Return exactly 2 expressions, at most 512 total nodes and depth at most 24.
Required object structure (replace the outputs placeholder with the expression list):
{"schema":"atomos-teacher-procedure-v1","profile_id":"full_adder_1bit_v1","procedures":[{"name":"full_adder_1bit_v1","input_bits":3,"output_bits":2,"outputs":"REPLACE_WITH_OUTPUT_EXPRESSION_LIST"}]}
