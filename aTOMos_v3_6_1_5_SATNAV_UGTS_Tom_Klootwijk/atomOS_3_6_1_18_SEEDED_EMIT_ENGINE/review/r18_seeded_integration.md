# R18 original seeded integration: CPU reference evidence

The original root was read only:

`C:/TOM/TOM_World_Query_Kernel_0_6_0_Tom_Klootwijk/TOM_World_Query_Kernel_0_6_0_Tom_Klootwijk`

`examples/seeded18/source_manifest.json` pins the exact copied inputs and original artifacts. Every imported vendor `src/python/tomagi/*.py` file was compared byte-for-byte with the original implementation before use. No compiler, formal evaluator, original fixture definition or external source file was edited.

The CPU build reproduced both fixtures with the original seeded entry point `compile_file_result`. Each `.tmg` and its complete compiler sidecar were generated twice, compared byte-for-byte to each other, and compared byte-for-byte to the original stored artifacts. The binary ABI decode/re-encode was identical. The source definition order, selected dependency closure, source receipts and cell crosswalk remain in the unchanged compiler reports.

| Fixture | Program bytes | Emissions | Artifact bytes | Host formal steps |
| --- | ---: | ---: | ---: | ---: |
| Original world03 release | 41,696 | 866 | 3,461 | No formal evaluator in this literal-byte fixture |
| Original learner06 family authority | 1,578,368 | 32,880 | 131,517 | 352,593 |

For the first fixture, the independently decoded literal bytes matched the original stored artifact and original CPU materializer. For the family fixture, the original formal evaluator was called separately with the declared source inputs and limits. Its canonical JSON plus LF matched both the original direct result and the original stored materialized result. Original Python VM execution followed by `materialize_trace` then reproduced every byte of that same result. Terminal HALT was checked rather than treating an authenticated prefix as a complete artifact.

All produced cells in these two seeded graphs are EMIT instructions. The source compiler computes the formal result on the host before constructing the emission graph. These checks therefore establish reproducible seeded compilation and complete reference output, not GPU execution of the formal AST or fresh native learner search.

`tools/verify_seeded_runtime.py` accepts separately generated native journal/output files. It checks complete status, zero fault, program flags/size, exact epoch bounds, every field of every ordered event, all sixteen final State64 words and every output byte against fresh original Python replay. Report hashes identify files in addition to these full comparisons; a digest alone is not the acceptance rule.

The nine CPU verifier/admission tests passed with:

```text
python -m unittest discover -s atomOS_3_6_1_18_SEEDED_EMIT_ENGINE/tests -p test_seeded_runtime.py -v
```

Their subcases reject alteration of each event field and each final state word, middle-byte/output truncation, journal truncation/duplication/reordering, prefix/fault and wrong epochs, Boolean-as-integer and duplicate JSON fields, a changed fixed seed, a stale definition hash, a rehashed missing dependency, and a changed source byte under its unchanged receipt. The positive verifier test uses an explicitly constructed CPU reference-shaped report; it is a verifier test and is not recorded as a GPU result.

Actual native runs and per-record/per-byte comparisons remain separate evidence files generated by the native CLI and verifier. This audit/fixture work ran no native build or GPU workload.
