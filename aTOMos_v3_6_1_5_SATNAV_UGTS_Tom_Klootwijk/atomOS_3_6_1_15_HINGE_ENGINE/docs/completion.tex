\section{Specialization and remaining engine scope}
The delivered core provides exact retained expressions, tested hinge and seam
state transitions, native texture execution, resident count certificates and
an explicit spatial comparison. Its intended applications include batched spherical selection,
directional sensor or scene indexing, guard-driven control state, procedural
spatial definitions and deterministic event replay. Resident immutable query
epochs and the tested count-to-word device loop provide a concrete path from
geometric decisions to delayed ASA/NA and JK state. The separate bounded
integer $\phi$ loop exercises self-referential operators across a banked
working set; its completed sizes and timings are recorded separately.
Each use selects the
units, frame, time source, equations and observable outputs it actually needs.

The retained Maxwell, mechanical, thermal, gravity and measurement equations
support physical specializations when their parameters, boundary data and
observation models are supplied. They do not supply an unmeasured material,
unknown satellite state or photonic transfer function. A spatial-index timing
does not establish orbital prediction accuracy or an optical implementation.

The remaining full-engine work includes lowering more of the authoritative
expression graph to native execution, certified transcendental resolution,
versioned dynamic geometry refit and rebinding of its derived caches,
full log-spherical LUT lifecycle measurements and broader nearest/region
operations against S2. The implemented immutable source/query epochs and
selected device feedback do not constitute that full dynamic lifecycle.
Every demanded value must resolve before its dependent hinge commits;
otherwise that hinge holds or refuses the event. These are implementation boundaries,
not additional physical laws added to the core.

The editable master is \code{docs/unified.tex}. The accompanying
\code{formal/} directory records the executable domains separately from the
inherited XOP specification. The \code{review/} directory records actual
checks, comparison inputs and measurements. Source hashes bind the retained
R14 foundation and immutable clock packet; the main-repository PDF has the
same bytes as the reviewed release PDF.
