# R21 packet paging

Continue the authorized POCO app work. Preserve R20 and prior releases unchanged.
Maintain package org.atomos.radio and signing key; upgrade in place, never uninstall.
User requests commits and pushes as validated stages and a PDF subversion.
Keep physical captures and identifying derivatives only in ignored private/ or sessions/.
This release removes the first-200 packet browser barrier using bounded, filtered pages.
Do not claim unlimited capture, decrypted payloads or live USB radio adapter capture.
PCAPdroid remains a separate explicit capture app; USB storage uses Android document import.
Root owns physical-phone operations; app agent may operate its own emulator.
